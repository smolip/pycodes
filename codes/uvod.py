import os
x = "has a number of functions and types built into it that are alwa"
#print(chr(max(ord(i) for i in x)))


y = "CG"
#print((y.count("C") / len(y)) * 100)¨

veta = "has a number of functions and types built into it that are alwa"
slovo= veta.split()
charStart = "i"

for x in slovo:
    if x.startswith(charStart):
        print(x)

#print(dir(os))

#pro datove typy list, set ,dict, str, int, tuple, jesli dodržují pořadí


def my_type(i):
    return hasattr(i, "index")

my_list =

print(hasattr("str"))